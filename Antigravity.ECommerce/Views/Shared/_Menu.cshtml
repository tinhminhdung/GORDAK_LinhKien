@using Antigravity.ECommerce.Models
@model List<Category>

<ul>
    @if (Model != null)
    {
        @foreach (var item in Model)
        {
            @await Html.PartialAsync("_MenuItem", item)
        }
    }
</ul>
